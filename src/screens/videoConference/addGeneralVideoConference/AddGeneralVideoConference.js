import { View, Text } from 'react-native';
import React from 'react';

const AddGeneralVideoConference = () => {
  return (
    <View>
      <Text>AddGeneralVideoConference</Text>
    </View>
  );
};

export default AddGeneralVideoConference;
